# Disable — formatter pads empty table cells (|  |  |) creating style conflicts
MD060: false

# Allow duplicate headings under different parent sections
MD024:
  siblings_only: true

# Disable line length rule — tables and long definitions exceed 80 chars
MD013: false
